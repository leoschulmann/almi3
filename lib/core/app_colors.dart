import 'package:flutter/material.dart';

class AppColors {
  static const Color verbMain = Color(0xFFFF006E);
  static const Color verbComplement = Color(0xFFF1F227);
  static const Color verbGradient = Color(0xFFFD01B6);

  static const Color adjectiveMain = Color(0xFFFFBE0B);
  static const Color adjectiveComplement = Color(0xFF802200);
  static const Color adjectiveGradient = Color(0xFFFFDE0A);

  static const Color nounMain = Color(0xFF8338EC);
  static const Color nounComplement = Color(0xFFC8F7C5);
  static const Color nounGradient = Color(0xFFB439ED);

  // Tekhelet — brand accent (saved/bookmarked state, nav icons)
  static const Color tekhelet = Color(0xFF1E5BB8);       // light mode
  static const Color tekheletDark = Color(0xFF5B9BFF);   // dark mode

  static const Color bookmark = Color(0xFFF71951);

  static const Color progressTrack = Color(0xFFE6E6EA);

  // page background (iOS systemGroupedBackground)
  static const Color pageBackground = Color(0xFFF2F2F7);

  // ink
  static const Color ink = Color(0xFF1C1C1E);
  static const Color inkSecondary = Color(0xFF8A8A8E);

  // input / control fills
  static const Color fieldFill = Color(0xFFE3E3E8);

  // translucent surfaces (nav bar, filter bar)
  static const Color navBarBackground = Color(0x8CF2F2F7);
  static const Color hairline = Color(0x14000000);
  static const Color filterThumbShadow = Color(0x1F000000);

  // card shadows
  static const Color cardShadowSoft = Color(0x12000000);

  // review pill
  static const Color pillBackground = Color(0x1FFF3B30);
  static const Color pillDot = Color(0xFFFF3B30);
  static const Color pillText = Color(0xFFD63A2F);

  static const Color textPrimary = Color(0xFF2D2D2D);
  static const Color textSecondary = Color(0xFF6D6C6C);
  static const Color bubbleBackground = Color(0xFFF2F2F2);
  static const Color cardBorder = Color(0xFFE0E0E0);
}
